class ColorUtils {
    static hexToRgb(hex) {
        //TODO: Convert hex color to RGB
    }

    static rgbToHex(r, g, b) {
        //TODO: Convert RGB to hex color
    }

    static lightenColor(hex, percent) {
        //TODO: Lighten color by percentage
    }

    static darkenColor(hex, percent) {
        //TODO: Darken color by percentage
    }

    static getContrastColor(hex) {
        //TODO: Get contrasting text color
    }

    static generateColorPalette(baseColor) {
        //TODO: Generate color palette from base color
    }
}

window.ColorUtils = ColorUtils;
