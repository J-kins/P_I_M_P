class Storage {
    static set(key, value) {
        //TODO: Set value in localStorage
    }

    static get(key, defaultValue = null) {
        //TODO: Get value from localStorage
    }

    static remove(key) {
        //TODO: Remove value from localStorage
    }

    static clear() {
        //TODO: Clear all localStorage
    }

    static setSession(key, value) {
        //TODO: Set value in sessionStorage
    }

    static getSession(key, defaultValue = null) {
        //TODO: Get value from sessionStorage
    }
}

window.Storage = Storage;
