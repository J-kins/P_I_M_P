class NumberUtils {
    static formatNumber(num) {
        //TODO: Format number with commas
    }

    static roundToDecimal(num, decimals = 2) {
        //TODO: Round number to decimal places
    }

    static calculatePercentage(part, total) {
        //TODO: Calculate percentage
    }

    static generateRandom(min, max) {
        //TODO: Generate random number in range
    }

    static formatLatLng(coord) {
        //TODO: Format latitude/longitude
    }
}

window.NumberUtils = NumberUtils;
