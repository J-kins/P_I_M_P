class UrlUtils {
    static getQueryParams() {
        //TODO: Parse URL query parameters
    }

    static updateQueryParam(key, value) {
        //TODO: Update URL query parameter
    }

    static getPathSegments() {
        //TODO: Get URL path segments
    }

    static isAbsoluteUrl(url) {
        //TODO: Check if URL is absolute
    }

    static buildUrl(path, params = {}) {
        //TODO: Build URL with parameters
    }
}

window.UrlUtils = UrlUtils;
